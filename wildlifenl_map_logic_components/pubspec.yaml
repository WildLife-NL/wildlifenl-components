name: wildlifenl_map_logic_components
description: WildLifeNL Map Logic Components – gedeelde kaartlogica (interfaces, Nederland-manager) voor Wild Rapport en WildManager.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  flutter_map: ^6.1.0
  latlong2: ^0.9.0
  geolocator: ^14.0.0
  geocoding: ^2.1.1

dev_dependencies:
  flutter_lints: ^5.0.0
