name: wildlifenl_authenticator_components
description: WildLifeNL Authenticator – token- en rolvalidatie, session clear. Gebruik samen met wildlifenl_login_components in Wild Rapport, WildManager en andere apps.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.5.3

dev_dependencies:
  flutter_lints: ^5.0.0
