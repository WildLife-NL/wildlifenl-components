name: wildlifenl_map_ui_components
description: WildLifeNL Map UI Components – shared UI (kleuren, knoppen, overlays, responsive) voor kaart/overview in Wild Rapport en andere WildLifeNL-apps.
version: 1.1.0
publish_to: 'none'

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^5.0.0
