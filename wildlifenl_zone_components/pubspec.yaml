name: wildlifenl_zone_components
description: Zone API and models for adding zones (POST /zone/). Usable with any backend that exposes the Zone API.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.7.0

dependencies:
  http: ^1.3.0
